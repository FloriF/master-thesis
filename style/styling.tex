% Overwrite/redefine figure environment for better figure placing
% adapted from https://tex.stackexchange.com/a/166458
% replaces standard '\begin{figure}[htbp]' with '\begin{figure}[H]'

\usepackage{float}

\let\origfigure\figure
\let\endorigfigure\endfigure
\renewenvironment{figure}[1][2] {
    \expandafter\origfigure\expandafter[H]
} {
    \endorigfigure
}

% add indentation at the start of every paragraph
\setlength\parindent{12pt}

% automatically generate short captions for figures.
% Take the first part of the caption until the first '.'
% short title/caption is used in toc

\usepackage{listofitems}
\usepackage{caption}

\usepackage{svg}

\newcommand\slcaption[1]{\setsepchar{.}\readlist*\pdots{#1}\caption[{\pdots[1].}]{#1}}
\captionsetup{format=hang}

% add running header/footer
% also remove uppercase from section names
\usepackage[markcase=noupper]{scrlayer-scrpage}
\automark[section]{section}
% footer: title of paper
\addtokomafont{pagefoot}{\sffamily}
\addtokomafont{pagehead}{\sffamily}
\ofoot*{Doorway Effect in Virtual Realtiy}
% \ohead{\headmark}

% \usepackage{fancyhdr}
% \fancyhead{}
% \fancyfoot{}
% \fancyfoot[LE,RO]{\thepage}

\usepackage{graphicx}
\usepackage[normalem]{ulem}
\useunder{\uline}{\ul}{}
\usepackage{rotating}
\usepackage{lscape}